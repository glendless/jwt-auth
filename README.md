# JWT Auth Demo using Node, Express and Mongoose

This is a demo project that implements JWT authentication using Node, Express and Mongoose. It provides a simple example of how to use JWT tokens for authentication and authorization in a Node application. The project uses Express for routing and Mongoose for database operations. It also uses the `jsonwebtoken` library for generating and verifying JWT tokens.
